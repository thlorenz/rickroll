{
  "name": "fanout",
  "version": "1.0.0",
  "main": "./dist/main.js",
  "scripts": {
    "esbuild": "esbuild src/main.ts --outdir=dist --platform=node --format=cjs",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.101",
    "@types/aws-sdk": "^2.7.0",
    "@types/node": "^18.0.6",
    "esbuild": "^0.14.49"
  },
  "license": "MIT",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1179.0"
  }
}
